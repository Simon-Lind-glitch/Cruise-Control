# Node deps — the CLI's bundled artifact (cli/dist/cc.js) IS committed; deps are not.
node_modules/

# OS / editor noise
.DS_Store

# Skills-CLI install state — local per clone, re-created by `cc init` / `npx skills add`.
.agents/
skills-lock.json
.claude/skills/
.claude/settings.local.json
